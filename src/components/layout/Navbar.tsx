// Top navbar for marketing pages (Landing, Pricing, About, Contact, Features, Tools)
// Reference: white navbar with Billora logo + Features/Pricing/Contact links + "Try it free" button

export default function Navbar() {
  return (
    <nav className="flex items-center justify-between px-7 py-4 bg-white border-b border-border">
      {/* TODO: logo + nav links + CTA button */}
    </nav>
  );
}
