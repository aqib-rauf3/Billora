// Receipt Maker Tool
// Reference mockup: billora_receipt_maker_tool.html
// Free tool — client-side form + live receipt preview, PDF export later

export default function ReceiptMakerTool() {
  return (
    <main className="min-h-screen p-8">
      <h1 className="text-2xl font-medium text-navy">Receipt Maker Tool — build me</h1>
    </main>
  );
}
