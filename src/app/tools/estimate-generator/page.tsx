// Estimate Generator Tool
// Reference mockup: billora_estimate_generator_tool.html
// Free tool — client-side line items + live preview, PDF export later

export default function EstimateGeneratorTool() {
  return (
    <main className="min-h-screen p-8">
      <h1 className="text-2xl font-medium text-navy">Estimate Generator Tool — build me</h1>
    </main>
  );
}
