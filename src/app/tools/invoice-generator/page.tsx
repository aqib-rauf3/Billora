// Invoice Generator Tool
// Reference mockup: billora_invoice_generator_tool.html
// Free tool — client-side line items + live preview, PDF export later

export default function InvoiceGeneratorTool() {
  return (
    <main className="min-h-screen p-8">
      <h1 className="text-2xl font-medium text-navy">Invoice Generator Tool — build me</h1>
    </main>
  );
}
