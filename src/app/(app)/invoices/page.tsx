// Invoice History
// Reference mockup: billora_invoice_history_page.html
// Search + status filter tabs + table — fetch from /api/invoices

export default function InvoiceHistoryPage() {
  return (
    <main className="min-h-screen p-8">
      <h1 className="text-2xl font-medium text-navy">Invoice History — build me</h1>
    </main>
  );
}
