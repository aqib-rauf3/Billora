// Create Invoice
// Reference mockup: billora_create_invoice_page.html
// Line items + live preview + save/send — POST to /api/invoices

export default function CreateInvoicePage() {
  return (
    <main className="min-h-screen p-8">
      <h1 className="text-2xl font-medium text-navy">Create Invoice — build me</h1>
    </main>
  );
}
