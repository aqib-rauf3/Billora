// Expenses (app page)
// Reference mockup: -
// List + add expenses — new page, mirror Customer Management pattern

export default function ExpensesPage() {
  return (
    <main className="min-h-screen p-8">
      <h1 className="text-2xl font-medium text-navy">Expenses (app page) — build me</h1>
    </main>
  );
}
