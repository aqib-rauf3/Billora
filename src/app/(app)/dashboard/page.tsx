// Dashboard
// Reference mockup: billora_dashboard_page.html
// Stat cards + recent invoices list — fetch from DB once API routes exist

export default function DashboardPage() {
  return (
    <main className="min-h-screen p-8">
      <h1 className="text-2xl font-medium text-navy">Dashboard — build me</h1>
    </main>
  );
}
