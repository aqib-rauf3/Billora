// Estimates (app page)
// Reference mockup: -
// List + create estimates — new page, no direct mockup yet, mirror Invoice History pattern

export default function EstimatesPage() {
  return (
    <main className="min-h-screen p-8">
      <h1 className="text-2xl font-medium text-navy">Estimates (app page) — build me</h1>
    </main>
  );
}
