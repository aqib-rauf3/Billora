// Landing Page
// Reference mockup: billora_landing_page_v2.html
// Sections: navbar, hero (headline + CTA + sample invoice card), stats strip, feature grid

export default function LandingPage() {
  return (
    <main className="min-h-screen">
      {/* TODO: Navbar */}
      {/* TODO: Hero section — headline, CTA buttons, sample invoice preview card */}
      {/* TODO: Stats strip (4 columns) */}
      {/* TODO: Feature grid (3 columns) */}
      <h1 className="text-2xl font-medium text-navy p-8">Landing Page — build me</h1>
    </main>
  );
}
